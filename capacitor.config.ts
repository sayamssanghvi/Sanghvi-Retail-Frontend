import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sanghviapps.retail',
  appName: 'Sanghvi Retail POS',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
